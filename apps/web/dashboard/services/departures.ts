import{api}from'./api';
export type Departure={id:string;departure_code:string;shipping_service_id:string;service_name:string;shipping_mode:string;route_name:string;origin_city?:string;origin_country?:string;destination_city?:string;destination_country?:string;scheduled_at:string;cutoff_at?:string;estimated_arrival_at?:string;actual_departure_at?:string;actual_arrival_at?:string;status:string;capacity_weight_kg?:number;reserved_weight_kg:number;capacity_cbm?:number;reserved_cbm:number;capacity_packages?:number;reserved_packages:number;shipment_count:number;carrier_name?:string;transport_reference?:string;timezone:string;responsible_name?:string;destination_office?:string;published:boolean;notes?:string;delay_reason?:string;checklist:Record<string,boolean>;row_version:number;allocations?:Array<Record<string,unknown>>;events?:Array<Record<string,unknown>>};
export type DepartureStats={today:number;this_week:number;confirmed:number;pending:number;delayed:number;full:number;packages:number;weight_kg:number;cbm:number};
export async function departures(params:Record<string,string>={}){return(await api.get<{items:Departure[]}>('/departures',{params})).data.items}export async function departureStats(){return(await api.get<DepartureStats>('/departures/stats')).data}export async function departure(id:string){return(await api.get<Departure>(`/departures/${id}`)).data}export async function createDeparture(p:Record<string,unknown>){return(await api.post<Departure>('/departures',p)).data}export async function allocateDeparture(id:string,p:Record<string,unknown>){return(await api.post(`/departures/${id}/allocations`,p)).data}export async function transitionDeparture(id:string,status:string,expected_version:number,reason?:string){return(await api.post<Departure>(`/departures/${id}/transition`,{status,expected_version,reason})).data}
