'use client';import{FormEvent,useCallback,useEffect,useState}from'react';import{Download,Megaphone,Plus,RefreshCcw,Search,X}from'lucide-react';import{OperationPageHeader}from'@/components/ui/operation-page-header';import{campaignAction,campaignResources,createCampaign,getCampaign,listCampaigns,saveAudience,snapshotCampaign,type Campaign}from'@/services/broadcasts';const btn='h-9 border border-[#d5d9dc] bg-white px-3 text-[12px] font-medium',primary='h-9 bg-[#16855f] px-3 text-[12px] font-semibold text-white',field='h-9 border px-3 text-[12px]';const tabs=[['','Toutes'],['DRAFT','Brouillons'],['PENDING_APPROVAL','À approuver'],['SCHEDULED','Programmées'],['SENDING','En cours'],['COMPLETED','Terminées'],['FAILED','Échouées']];export function BroadcastsPage(){const[items,setItems]=useState<Campaign[]>([]),[stats,setStats]=useState<Record<string,number>>({}),[status,setStatus]=useState(''),[q,setQ]=useState(''),[selected,setSelected]=useState<Campaign|null>(null),[modal,setModal]=useState<'campaign'|'audience'|null>(null),[resources,setResources]=useState<Record<string,unknown>>({}),[error,setError]=useState('');const load=useCallback(()=>listCampaigns({q,status}).then(r=>{setItems(r.items);setStats(r.stats)}).catch(()=>setError('Campaign Engine indisponible.')),[q,status]);useEffect(()=>{load();campaignResources().then(setResources)},[load]);async function open(x:Campaign){setSelected((await getCampaign(x.id)).campaign)}async function act(a:string){if(!selected)return;try{await campaignAction(selected.id,a,selected.row_version);setSelected(null);load()}catch{setError('Pré-check refusé, droits insuffisants ou campagne déjà modifiée.')}}const cards=[['Actives',stats.active],['Programmées',stats.scheduled],['Envoyées ce mois',stats.sent_month],['Destinataires',stats.recipients],['Délivrés',stats.delivered],['Lus',stats.read],['Réponses',stats.replies],['Taux lecture',`${stats.read_rate||0}%`]];return <div className="min-h-full bg-[#f7f7f6]"><OperationPageHeader title="Broadcasts" description="Créez, programmez et analysez vos campagnes WhatsApp et email auprès de vos clients et prospects." actions={<><button className={btn} onClick={()=>setModal('audience')}>Créer une audience</button><button className={primary} onClick={()=>setModal('campaign')}><Plus size={14} className="inline"/> Nouvelle campagne</button></>} tabs={<div className="flex">{tabs.map(([v,l])=><button key={v} onClick={()=>setStatus(v)} className={`h-9 border-b-2 px-3 text-[12px] ${status===v?'border-[#16855f] font-semibold':'border-transparent'}`}>{l}</button>)}</div>}/><section className="grid border-b bg-white md:grid-cols-4 xl:grid-cols-8">{cards.map(([l,v])=><div key={l} className="border-r p-4"><small>{l}</small><b className="mt-2 block text-xl">{v||0}</b></div>)}</section><div className="flex gap-2 border-b bg-white p-4"><label className="flex h-9 flex-1 items-center border px-3"><Search size={14}/><input className="ml-2 flex-1 outline-none" value={q} onChange={e=>setQ(e.target.value)} placeholder="Rechercher une campagne..."/></label><button className={btn} onClick={load}><RefreshCcw size={14}/></button><button className={btn}><Download size={14}/></button></div>{error&&<p className="m-4 bg-red-50 p-3 text-red-700">{error}</p>}<table className="w-full bg-white text-left text-[12px]"><thead><tr>{['Campagne','Canaux','Audience','Statut','Programmation','Performance'].map(x=><th className="border-b p-3" key={x}>{x}</th>)}</tr></thead><tbody>{items.map(x=><tr onClick={()=>open(x)} className="cursor-pointer border-b hover:bg-[#fafafa]" key={x.id}><td className="p-3"><b>{x.title}</b><small className="block">{x.reference}</small></td><td>{x.channels?.join(' + ')}</td><td>{x.recipients||0}</td><td>{x.status}</td><td>{x.scheduled_at?new Date(x.scheduled_at).toLocaleString('fr-FR'):'—'}</td><td>{x.reads||0} lus · {x.replies||0} réponses</td></tr>)}</tbody></table>{selected&&<aside className="fixed inset-y-0 right-0 z-50 w-full max-w-xl overflow-y-auto bg-white p-5 shadow-2xl"><button className="float-right" onClick={()=>setSelected(null)}><X/></button><Megaphone/><h2 className="mt-3 text-xl font-semibold">{selected.title}</h2><p>{selected.reference} · {selected.status}</p><div className="my-5 border p-4 whitespace-pre-wrap">{selected.message}</div><div className="flex flex-wrap gap-2"><button className={btn} onClick={async()=>{await snapshotCampaign(selected.id);setSelected((await getCampaign(selected.id)).campaign)}}>Calculer & figer audience</button><button className={btn} onClick={()=>act('APPROVE')}>Approuver</button><button className={primary} onClick={()=>act('START')}>Lancer</button><button className={btn} onClick={()=>act('PAUSE')}>Pause</button><button className={btn} onClick={()=>act('RESUME')}>Reprendre</button><button className={btn} onClick={()=>act('CANCEL')}>Annuler</button></div></aside>}{modal==='campaign'&&<CampaignModal resources={resources} close={()=>setModal(null)} done={()=>{setModal(null);load()}}/>}{modal==='audience'&&<AudienceModal close={()=>setModal(null)} done={()=>{setModal(null);campaignResources().then(setResources)}}/>}</div>}
function CampaignModal({resources,close,done}:{resources:Record<string,unknown>;close:()=>void;done:()=>void}){async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await createCampaign({title:f.get('title'),message:f.get('message'),campaign_type:f.get('type'),objective:f.get('objective'),channels:[f.get('channel')],audience_id:f.get('audience')||null,scheduled_at:f.get('date')?new Date(String(f.get('date'))).toISOString():null,timezone_mode:'WORKSPACE',language_versions:{},media:[],variable_defaults:{client_name:'Client'}});done()}return <Modal title="Nouvelle campagne" close={close}><form className="grid gap-3" onSubmit={submit}><input required name="title" className={field} placeholder="Titre campagne"/><select name="objective" className={field}><option>INFORM</option><option>PROMOTE</option><option>REACTIVATE</option><option>ANNOUNCE</option></select><select name="type" className={field}><option value="INFORMATIONAL">Information opérationnelle</option><option value="COMMERCIAL">Commerciale — consentement obligatoire</option><option value="ENGAGEMENT">Engagement</option></select><select name="channel" className={field}><option>WHATSAPP</option><option>EMAIL</option></select><select name="audience" className={field}><option value="">Choisir audience</option>{((resources.audiences||[])as Array<Record<string,unknown>>).map(x=><option key={String(x.id)} value={String(x.id)}>{String(x.name)}</option>)}</select><textarea required name="message" className="min-h-36 border p-3" placeholder="Bonjour {{first_name}}, ..."/><input name="date" type="datetime-local" className={field}/><button className={primary}>Créer la campagne</button></form></Modal>}
function AudienceModal({close,done}:{close:()=>void;done:()=>void}){async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await saveAudience({name:f.get('name'),audience_type:'DYNAMIC',workspace_id:null,filter_config:{country:f.get('country')||undefined,language:f.get('language')||undefined,status:f.get('status')||undefined}});done()}return <Modal title="Audience dynamique" close={close}><form className="grid gap-3" onSubmit={submit}><input required name="name" className={field} placeholder="Clients Air Cargo RDC"/><input name="country" className={field} placeholder="Pays, ex. RDC"/><select name="language" className={field}><option value="">Toutes langues</option><option>FR</option><option>EN</option></select><select name="status" className={field}><option value="">Tous statuts</option><option>ACTIVE</option><option>LEAD</option></select><button className={primary}>Enregistrer</button></form></Modal>};function Modal({title,close,children}:{title:string;close:()=>void;children:React.ReactNode}){return <div className="fixed inset-0 z-50 bg-black/20" onClick={close}><section onClick={e=>e.stopPropagation()} className="ml-auto h-full w-full max-w-lg bg-white p-5"><button className="float-right" onClick={close}><X/></button><h2 className="mb-5 text-xl font-semibold">{title}</h2>{children}</section></div>}
